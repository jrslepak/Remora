#lang racket

; TODO:
; #A(NAT ...)(ATOM ...)
;  reads as
; (alit (NAT ...) ATOM ...)

; TODO:
; [ALITERAL ...]
;  reads as
; (array ALITERAL ...)

; TODO:
; #r(RANK ...)EXP
;  reads as
; (rerank (RANK ...) EXP)

